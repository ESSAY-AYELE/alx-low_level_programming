#include <string.h>

/**
 * _strcpy - copy one string to onther arry
 * @dest: copied to
 * @src: copied from
 *Return: the dest array
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
