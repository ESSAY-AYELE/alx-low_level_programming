#include <string.h>
/**
 * _strcat - concatenates to strings
 * @dest: the fitst string
 * @src: the second string
 * Return: the mereged string
 *
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest,src));
}
