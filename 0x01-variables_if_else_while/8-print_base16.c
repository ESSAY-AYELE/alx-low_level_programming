#include <stdio.h>
/**
 * main -Entry poit
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int c;

	for (c = 48; c < 'g' ; c++)
	{
		putchar(c);
    if (c == 57)
    c=='a'-1
	}
	putchar('\n');
	return (0);
}
