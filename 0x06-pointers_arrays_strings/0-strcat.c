#include "main.h"
/**
 * _strcat - concatenate the strings
 * @src: source
 * @dest: destination
 * Return: pointer to the string
 */
char *_strcat(char *dest, char *src)
{
	dest = dest + src;
	return dest;
}
